name: bigquery_lab
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - pyarrow
      - google-cloud-bigquery
      - db-dtypes
      - jupyter
      - faker
      - matplotlib
      - seaborn